# DYNAMIC_KINSHIP_POLICY_SOURCE_MAP

## Purpose

Record the exact source artifacts used to implement and brief the dynamic Kinship policy configuration and broader admitted inspector coverage lane.

## Strategic sources

### `ops/strategies/agent-workforce/AGENT_WORKFORCE_OPERATIONALIZATION_STRATEGY.md`

Used to preserve:

- ACP as runtime authority
- A2A as bounded coordination instead of unconstrained agent mesh behavior
- the requirement that operator visibility must not become transport authority

### `ops/missions/agent-workforce/ACP_NATIVE_TRANSPORT_BRIEF.md`

Used to carry forward:

- Kinship-governed ACP transport as already-ratified law
- the need to stamp and preserve admitted route-law truth through transport
- the earlier requirement that transport stay governed by policy rather than conversational inference

### `ops/missions/agent-workforce/KINSHIP_POLICY_ENGINE_BRIEF.md`

Used to carry forward:

- the consolidated Kinship Policy Engine as the current transport authority seam
- the explicit next-lane requirement to add dynamic policy configuration
- admitted inspector subscriptions as the existing live operator seam

## Kinship and memory sources

### `../../intake/CYBORGClaW_Memory_Intake_Batch_01/CYBORGClaW/ACP SSOT/cyborgclaw_developer_ssot_v1_2.md`

Used to preserve:

- Kinship as governing architecture
- route legality as admitted policy truth
- cousin mediation and artifact-return obligations as transport law

### `../../intake/CYBORGClaW_Memory_Intake_Batch_01/CYBORGClaW/normalized/strategy/cyborgclaw-neov2-architecture-truth-and-deployment-strategy.extracted.md`

Used to reinforce:

- gateway-first runtime truth
- Kinship-first organizational routing
- ACP internal transport with A2A exposed as bounded edge behavior

## OpenClaw architecture sources

### `docs/architecture/kinship-route-classification.md`

Used to preserve:

- route classes as frozen legal categories
- route classification as explicit governance truth

### `docs/architecture/cousin-ticket-law.md`

Used to preserve:

- cousin routing requires explicit mediated ticket truth
- cross-President work remains ticketed and governed

### `docs/architecture/lineage-admission-rules.md`

Used to preserve:

- admitted lineage/runtime/policy truth as the execution gate
- public receipts remain reduced relative to richer internal admission truth

## OpenClaw implementation sources

### `src/acp/control-plane/kinship-policy-engine.ts`

Used to identify:

- the already-consolidated Kinship admission seam
- the route-law bundle resolution path
- the transport admission path that needed dynamic policy loading and snapshot stamping

### `src/acp/control-plane/manager.core.ts`

Used to preserve:

- ACP admission flow around the policy engine
- route-law bundle consumption during session setup and inter-session transport

### `src/config/sessions/types.ts`

Used to extend:

- transport policy snapshot detail with dynamic policy metadata

## Workforce product sources

### `../sprytly-internal-dashboard/server/src/index.js`

Used to implement:

- broader admitted inspector subscription normalization
- policy and lifecycle event coverage on the live stream
- Kinship policy read/write APIs
- policy event emission and cockpit-facing read models

### `../sprytly-internal-dashboard/server/src/workforceJobDispatch.js`

Used to preserve:

- Workforce transport policy shaping and receipt generation
- policy-engine identity and version truth on durable runtime receipts

### `../sprytly-internal-dashboard/server/src/memoryVaultIntakeBatch.js`

Used to improve:

- Kinship metadata tagging on durable and Tier 2 ACP artifacts

### `../sprytly-internal-dashboard/server/src/memoryVaultSearch.js`

Used to improve:

- runtime `memory.search` indexing over governance-oriented metadata fields

### `../sprytly-internal-dashboard/web/src/pages/MissionControlWorkforceAlphaPage.jsx`

Used to land:

- broader admitted inspector subscription scopes
- live policy summary visibility
- route-class cards
- recent policy and lifecycle receipt rendering in the inspector and transport activity feed

## Validation sources

### `src/acp/control-plane/kinship-policy-config.test.ts`

Used to prove:

- fallback to the baked-in Kinship policy floor
- tightening-only dynamic policy normalization

### `src/acp/control-plane/kinship-policy-engine.test.ts`

Used to prove:

- dynamic policy hot reload without restart
- file-backed policy snapshot stamping
- active policy rejection behavior for transport admission

### `src/acp/control-plane/manager.test.ts`

Used to prove:

- admitted transport metadata now carries the richer dynamic policy snapshot

### `../sprytly-internal-dashboard/scripts/tests/workforce_kinship_policy.test.mjs`

Used to prove:

- workforce-side mirrored policy helpers preserve the same tightening-only semantics

### `../sprytly-internal-dashboard/scripts/tests/workforce_job_dispatch_api.test.mjs`

Used to prove:

- admitted inspector subscriptions receive broader `transport`, `policy`, and `lifecycle` coverage
- connected envelopes include live Kinship policy truth
- policy updates emit `workforce.transport.policy_config.updated`
- pushed inspector payloads refresh after policy changes

### `../sprytly-internal-dashboard/scripts/tests/memory_vault_search.test.mjs`

Used to prove:

- runtime search recalls Kinship ACP artifacts through governance metadata

### `../sprytly-internal-dashboard/scripts/tests/memory_vault_multibatch_api.test.mjs`

Used to prove:

- API-level Memory HQ search also recalls Kinship ACP artifacts through the improved indexing path

## Implementation conclusion

This lane is grounded in already-ratified Kinship transport law and extends it in three tightly bounded ways:

- Kinship policy can now tighten dynamically from a live config source
- admitted inspector subscriptions now carry broader policy and lifecycle truth
- Memory HQ now has better recall for the Kinship governance artifacts that define this transport model

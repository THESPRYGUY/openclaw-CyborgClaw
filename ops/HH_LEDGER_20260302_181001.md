# CyborgClaw H&H Ledger Entry

- When: Mon Mar 2 06:10:01 PM UTC 2026 (local) / Mon Mar 2 06:10:01 PM UTC 2026 (UTC)
- Change: Hardened `~/bin/cyborg-run health` to **assert** strike_echo is pinned to `openai-codex/gpt-5.3-codex`.
- Why: Prevent silent provider failover (e.g., Google) from masking Codex auth/routing issues during smoke tests.
- Proof (latest): `~/bin/cyborg-run health` => strike_echo PASS 10/10 + `PIN_OK`.
- Backup: `~/bin/cyborg-run.bak.1772474680`

## Do Not Repeat

- Don’t use `openclaw systemctl ...` (use `systemctl` directly).
- Don’t treat WS 1006 during restart as broken — rerun after service is active.

## Next single action (next session)

- `~/bin/cyborg-run health`
